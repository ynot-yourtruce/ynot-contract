pragma solidity >0.8.0;

contract Stats{

    mapping(address => mapping( address => uint256)) private pool_volume_1;
    mapping(address => mapping( address => uint256)) private pool_volume_2;

    
}